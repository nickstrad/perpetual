# Go build artifacts and coverage reports
/bin/
*.test
*.out
coverage.*

# Local Go workspace configuration
go.work
go.work.sum

# Local editor and operating-system files
.idea/
.vscode/
.DS_Store

# Per-work state files: append-only activity notes for resuming across context clears and AI tools.
# Never committed; see "Working state" in CLAUDE.md.
/.state/

# Local generated PR reading companions.
/.scratchpad/
/.runtime/
/artifacts/
